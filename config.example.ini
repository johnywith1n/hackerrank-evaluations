[App]
SECRET_KEY = replace with real (as base 64 string)
BOOTSTRAP_ADMIN = replace with the email of the user you want to become an admin upon creation
ALLOWED_EMAIL_DOMAIN = replace with the allowed email domain

[Hackerrank]
API_KEY = replace with real

[Google]
CLIENT_SECRET_FILENAME = replace with real